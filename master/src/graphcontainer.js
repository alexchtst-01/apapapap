import React from "react";
import LineChart from "./lineplot";

const GraphContainer = () => {
  // Placeholder for dynamic graphs
  return (
    <div>
      <LineChart area={'AreaX'} />
    </div>
  );
};

export default GraphContainer;
